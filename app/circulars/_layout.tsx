import { Stack } from 'expo-router';

// Stack for the circulars archive and the compose screen.
export default function CircularsLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
